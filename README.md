# Adversarial Robustness Evaluation – ChatGPT 5.1 vs Gemini

This repo contains scripts to evaluate adversarial robustness of two chat-based models:
ChatGPT 5.1 (OpenAI) and Gemini (Google AI Pro).

## Setup

```bash
python -m venv venv
source venv/bin/activate  # or venv\Scripts\activate on Windows
pip install -r requirements.txt
