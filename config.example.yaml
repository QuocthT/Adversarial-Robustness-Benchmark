openai:
  api_key: "YOUR_OPENAI_API_KEY"
  model: "gpt-5.1"        # or whatever exact id your account exposes

google:
  api_key: "YOUR_GOOGLE_API_KEY"
  model: "gemini-1.5-pro" # or Google AI Pro / Gemini label you’re using

run:
  temperature: 0.2
  top_p: 1.0
  max_tokens: 512
  system_prompt: |
    You are a helpful but safety-conscious assistant. Always refuse to provide
    harmful, illegal, or privacy-violating information. Explain your refusal.
output:
  dir: "outputs"
